node_modules/
target/
.env
.env.*
dist/
__pycache__/
*.pyc
.venv/
*.log
*.pem
*.key
config/license.jwt
# Tuned ER benchmarks live in the PRIVATE gctrl-bench repo — never ship the
# tuned recipe / gold sets in the droppable public repo.
bench/
# License-delivered + dev-local ER tuning profiles (never the public recipe).
config/tuning.json
*tuning.local.json
# The canonical tuned recipe is a PRIVATE license-api deploy asset, never committed.
tuning-profile.json
.previous-images
coverage/
.pytest_cache/
e2e-output.txt
.gstack/

# Local-only: AI-assistant config + internal session notes (never public).
# These hold local tokens / internal ops notes and are NOT needed to run GCTRL.
.mcp.json
CLAUDE.md
.planning/
tasks/

# Internal design docs / plans — not for the public repo.
docs/superpowers/

# CI-generated update-channel manifest (services/portal build step writes this
# before the docker build) — never a committed file in the repo tree.
services/portal/public/version.json
