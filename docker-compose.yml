version: "3.8"

services:
  api:
    build: ./services/api
    container_name: gctrl-api
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=development
      - PORT=4000
      - DATABASE_URL=postgresql://GCTRL:GCTRL@postgres:5432/GCTRL
      - REDIS_URL=redis://redis:6379
      - NEO4J_URI=bolt://neo4j:7687
      - NEO4J_USER=neo4j
      - NEO4J_PASSWORD=password
      - JWT_SECRET=${JWT_SECRET:-GCTRL-dev-jwt-secret-change-in-production}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET:-GCTRL-dev-refresh-secret-change-in-production}
      - MAIL_HOST=maildev
      - MAIL_PORT=1025
      - FRONTEND_URL=http://localhost:3000
      - KEX_WORKER_URL=http://kex:4010
      - QDRANT_URL=http://qdrant:6333
      - OLLAMA_BASE=http://ollama:11434
      # OAuth Connectors (set in .env or override here)
      - GCTRL_BASE_URL=${GCTRL_BASE_URL:-http://localhost:4000}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET:-}
      - MICROSOFT_CLIENT_ID=${MICROSOFT_CLIENT_ID:-}
      - MICROSOFT_CLIENT_SECRET=${MICROSOFT_CLIENT_SECRET:-}
      - SLACK_CLIENT_ID=${SLACK_CLIENT_ID:-}
      - SLACK_CLIENT_SECRET=${SLACK_CLIENT_SECRET:-}
      - GITHUB_CLIENT_ID=${GITHUB_CLIENT_ID:-}
      - GITHUB_CLIENT_SECRET=${GITHUB_CLIENT_SECRET:-}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - GCTRL
      - ollama-net
    restart: unless-stopped

  web:
    build: ./services/web
    container_name: gctrl-web
    ports:
      - "3001:80"
    depends_on:
      - api
    networks:
      - GCTRL
    restart: unless-stopped

  gctrl-agent:
    image: ghcr.io/gctrl/agent:latest
    container_name: gctrl-agent
    ports:
      - "127.0.0.1:7070:7070"
    volumes:
      - ./config:/app/config:ro
    environment:
      - GCTRL_LICENSE_JWT_PATH=/app/config/license.jwt
      - GCTRL_LICENSE_PUBLIC_KEY=${GCTRL_LICENSE_PUBLIC_KEY}
      - GCTRL_API_URL=https://api.gctrl.tech
    networks:
      - GCTRL
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:7070/status"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped

  kex:
    build: ./services/kex
    container_name: gctrl-kex
    ports:
      - "4010:4010"
    environment:
      - OLLAMA_BASE=http://ollama:11434
      - NEO4J_URI=bolt://neo4j:7687
      - NEO4J_USER=neo4j
      - NEO4J_PASSWORD=password
      - REDIS_URL=redis://redis:6379
      - QDRANT_URL=http://qdrant:6333
      - QDRANT_COLLECTION=GCTRL_chunks
      - EMBEDDING_MODEL=nomic-embed-text
      - PG_URL=postgresql://GCTRL:GCTRL@postgres:5432/GCTRL
      - PORT=4010
    volumes:
      - kex-models:/app/models
    depends_on:
      redis:
        condition: service_started
      gctrl-agent:
        condition: service_healthy
    networks:
      - GCTRL
      - ollama-net
    restart: unless-stopped
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

  fuse:
    build: ./services/fuse
    container_name: gctrl-fuse
    ports:
      - "4020:4020"
    environment:
      - NEO4J_URI=bolt://neo4j:7687
      - NEO4J_USER=neo4j
      - NEO4J_PASSWORD=password
      - REDIS_URL=redis://redis:6379
      - PORT=4020
      - SIMILARITY_THRESHOLD=0.85
      - LIMES_URL=http://limes:8080
    depends_on:
      redis:
        condition: service_started
      gctrl-agent:
        condition: service_healthy
    networks:
      - GCTRL
    restart: unless-stopped

  limes:
    build:
      context: ../DataBorg Github/LIMES-master
      dockerfile: Dockerfile
    container_name: gctrl-limes
    ports:
      - "8085:8080"
    environment:
      - JAVA_OPTS=-Xmx2G
    command: ["-s"]
    networks:
      - GCTRL
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    container_name: gctrl-postgres
    ports:
      - "5433:5432"
    environment:
      - POSTGRES_USER=GCTRL
      - POSTGRES_PASSWORD=GCTRL
      - POSTGRES_DB=GCTRL
    volumes:
      - GCTRL-pgdata:/var/lib/postgresql/data
      - ./shared/db/migrations:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U GCTRL"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - GCTRL
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: gctrl-redis
    ports:
      - "6380:6379"
    networks:
      - GCTRL
    restart: unless-stopped

  maildev:
    image: maildev/maildev
    container_name: gctrl-maildev
    ports:
      - "1081:1080"
      - "1026:1025"
    networks:
      - GCTRL
    restart: unless-stopped

volumes:
  GCTRL-pgdata:
  kex-models:

networks:
  GCTRL:
    driver: bridge
  ollama-net:
    external: true
    name: self-hosted-ai-starter-kit_demo


